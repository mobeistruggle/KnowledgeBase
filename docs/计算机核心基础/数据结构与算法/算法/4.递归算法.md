## 递归算法

递归算法（Recursive Algorithm）是一种通过自身重复调用来解决问题的算法

```python
# 解决简单的阶乘

def factorial(n):
    if n == 0 or n == 1:  # 基本情况
        return 1
    else:  # 递归步骤
        return n * factorial(n - 1)

result = factorial(5)
print(result)  # 输出结果为 120
```

